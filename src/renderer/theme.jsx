import colors from './colors';

const theme = {
  backgroundColor: colors.black.verydark,
  sidenavBackground: colors.black.dark,
  sideNavItemBackground: colors.black.light,
};

export default theme;
